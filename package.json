{
  "name": "en-controle",
  "version": "0.0.0",
  "private": true,
  "description": "EN Controle — controle de valores a receber. Não é empacotamento: o produto é servido como arquivos estáticos da raiz.",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
